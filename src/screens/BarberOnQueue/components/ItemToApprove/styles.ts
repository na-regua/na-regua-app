import {Colors} from '@/theme';
import styled from 'styled-components/native';

export const ToApproveContainerStyled = styled.View`
  padding: 12px;
  border-radius: 12px;
  background-color: ${Colors.primary};
  gap: 12px;
`;

export const ToApproveUserStyled = styled.View`
  flex-direction: row;
  align-items: center;
  gap: 12px;
`;

export const ToApproveActionStyled = styled.View`
  flex-direction: row;
  align-items: center;
  gap: 12px;
`;
