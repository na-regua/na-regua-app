import {StyleSheet} from 'react-native';

export const addWorkerStyles = StyleSheet.create({
  container: {
    gap: 18,
    flex: 1,
  },
  avatarWrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
