import styled from 'styled-components/native';

const ModalContainerStyle = styled.View`
  flex-direction: column;
  gap: 18px;
`;

const ModalActionsStyle = styled.View`
  flex-direction: row;
  gap: 18px;
  flex: 1;
`;

export default {
  ModalContainerStyle,
  ModalActionsStyle,
};
